{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "CMake: build",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "CMake build task for embedded-memory-param-manager"
        }
    ],
    "version": "2.0.0"
}
